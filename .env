# Server Configuration
PORT=3000

# App Environment
NODE_ENV=production

# API Credentials (used in request headers)
CHANNEL_ID=your_channel_id
USER_ID=your_user_id
PASSWORD=your_password
TERMINAL_ID=your_terminal_id
